# Server-only Google Places API credentials.
# Do not expose these values through NEXT_PUBLIC_* variables or commit .env.local.
GOOGLE_MAPS_API_KEY=
GOOGLE_PLACE_ID=

# Server-only Resend credentials for the contact form.
# Add the real values in .env.local and Vercel Environment Variables.
RESEND_API_KEY=
CONTACT_TO_EMAIL=insectokomarnici@gmail.com
