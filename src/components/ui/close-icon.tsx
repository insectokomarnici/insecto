import { X } from "@boxicons/react";
import type { BoxIconProps } from "@boxicons/react";

/** Boxicons close icon cropped to its visible bounds. */
export function CloseIcon(props: BoxIconProps) {
  return <X removePadding {...props} />;
}
